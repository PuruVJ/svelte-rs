//! Static lookup tables for ARIA roles, properties, and AXObject mappings.
//!
//! Data sourced byte-for-byte from `aria-query` and `axobject-query` npm packages
//! (versions pinned in `packages/svelte/package.json`).

#![forbid(unsafe_code)]
