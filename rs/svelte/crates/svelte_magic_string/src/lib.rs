//! Port of the `magic-string` npm package.
//!
//! Source of truth: the upstream library's behavior as exercised by
//! `packages/svelte/src/compiler/` and the `packages/svelte/tests/sourcemaps/`
//! fixture suite. Do not paraphrase API semantics — derive them from observed output.

#![forbid(unsafe_code)]
