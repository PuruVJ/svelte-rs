[package]
name = "svelte_magic_string"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Port of the `magic-string` npm package: chunked string with sourcemap-tracked mutations."

[dependencies]
sourcemap = { workspace = true }
